<li class="mb-4">
    {% if showQuestion|default(false) %}
        <a
            href="{{  path('app_question_show', {
                slugulusErecto:answer.question.slug
            }) }}"
            class="mb-1 link-secondary">
            <strong>Question:</strong>
            {{ answer.questionText |u.truncate(80,'...')}}
        </a>
    {% endif %}
    <div class="d-flex justify-content-center">
        <div class="mr-2 pt-2">
            <img src="{{ asset('images/tisha.png') }}" width="50" height="50">
        </div>
        <div class="mr-3 pt-2">
            {{ answer.content }}
            <p>-- {{ answer.username }}</p>
        </div>
        <div class="col-2 text-end">
            <small>{{ answer.createdAt |ago }}</small>

            {# <form action="{{ path('app_question_vote', { slugulusErecto: question.slug }) }}" method="POST"> #}
                <div class="vote-arrows flex-fill pt-2 js-vote-arrows" style="min-width: 90px;">
                    <button class="vote-up btn btn-link" name="direction" value="up"><i class="far fa-arrow-alt-circle-up"></i></button>
                    <button class="vote-down btn btn-link" name="direction" value="down"><i class="far fa-arrow-alt-circle-down"></i></button>
                    <span>{{ answer.votes }}</span>
                </div>
            {# </form> #}
        </div>
        {#<div class="vote-arrows flex-fill pt-2 js-vote-arrows" style="min-width: 90px;">
                            <a class="vote-up" href="#" data-direction="up"><i class="far fa-arrow-alt-circle-up"></i></a>
                            <a class="vote-down" href="#" data-direction="down"><i class="far fa-arrow-alt-circle-down"></i></a>
                            <span>{{ question.votesString }}</span>
                        </div>#}
    </div>
</li>